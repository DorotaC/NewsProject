import { updateUI } from '../src/client/js/updateUI'

describe("Update UI checker", () => {
  test("This test should check if function updateUI exists", () => {
    expect(updateUI).toBeDefined()
  })
})
